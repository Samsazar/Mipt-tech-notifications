# Mipt-tech-notifications

## Инструкция

Здравствуйте, представляю вам свой небольшой проект. Для того, чтобы активировать бота, вам будет необходимо выполнить следующие шаги:
1. Телеграм бот. Нужно его сделать самостоятельно через BotFather и получить токен. 
2. Необходимо создать файл .env. Я бы сказал как создать его через cmd на винде, но боюсь, что ошибусь, т.к. у самого macOS, поэтому объясняться буду через консоль bash
Для начала необходимо прописать команду "touch .env", находясь в директории проекта. Далее написать "echo "API_TOKEN=<ваш токен>" > .env"
После этого пункта неободимо создать виртуальное окружение и скачать все необходимые библиотеки. Для этого понадобится ввести 3 команды
"python -m venv venv" - создали venv
"source venv/bin/activate" - используем его
"pip install -r requirements.txt" - устанавливаем библиотеки
И наконец запускаем программу
"python main.py"
В боте описано все, что нужно делать.
Enjoy

## Документация

Данный проект осуществляет возможность получения уведомления от mipt.tech о бронировании стиральной машинки в общежитии,
телеграм бот будет присылать напоминания о том, что скоро стирка. Для этого необходимо будет войти в 
свой аккаунт через бота и сделать запрос на получение уведомления

Класс Бота, который будет выполнять все необходимые функции
1. Старт бота
2. Команда - Обновление данных о стирке, инструкция, настройка уведомлений
Класс Базы данных, который будет возвращать и получать значения
1. Получить данные о пользователе 
2. Записать пользователя
3. Получить данные о времени стирки
4. Записать данные о времени стирки
Класс Диспетчер, который будет присылать сообщения от пользователя
1. Функция прослушивания (Polling)
Класс Сообщения, который будет присылать Диспетчер. Хранит в себе класс Пользователя, текст и другие данные о сообщении
Класс Пользователя, который хранит в себе id, имя, фамилию пользователя
Класс Обработчика, который будет проверять сообщения на наличие какого-то подтекста
1. Функция, которая будет возвращать булевое значение при проверке сообщения на нужную зависимость
 

## UPD:
Для реализации таймера не хватило времени, т.к. необходимо было разобраться с еще одной библиотекой schedule, на которую не хватило времени (необходимо было разбираться с асинхронностью и созданием корутины "расписания")
Я двое суток разбирался, но на выходе получаю, что расписание с самим ботом не очень дружат, поэтому решил сделать просто бота с напоминаниями, где можно посмотреть свои стиралки. Все же функционал похуже, чем необходимый, но тем не менее он имеет смысл, т.к. заходить на mipt.tech ради этого сложнее, чем зайти в тг

